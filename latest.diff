diff --git a/src/val_common_peripherals.c b/src/val_common_peripherals.c
index de40363..e32f585 100644
--- a/src/val_common_peripherals.c
+++ b/src/val_common_peripherals.c
@@ -34,6 +34,12 @@ static void val_unlock(atomic_flag *lock)
 
 static bool val_nvm_range_is_valid(size_t offset, size_t size)
 {
+#ifdef PLATFORM_NVM_SIZE
+    const size_t nvm_size = (size_t)PLATFORM_NVM_SIZE;
+#else
+    const size_t nvm_size = SIZE_MAX;
+#endif
+
     if (size > SIZE_MAX - offset) {
         return false;
     }
@@ -50,6 +56,14 @@ static bool val_nvm_range_is_valid(size_t offset, size_t size)
         return false;
     }
 
+    if (offset > nvm_size) {
+        return false;
+    }
+
+    if (size > (nvm_size - offset)) {
+        return false;
+    }
+
     return true;
 }
 
@@ -68,6 +82,10 @@ uint32_t val_nvm_read(size_t offset, void *buffer, size_t size)
           return VAL_STATUS_INVALID;
       }
 
+      if (size == 0U) {
+          return VAL_SUCCESS;
+      }
+
       val_lock(&nvm_lock);
       status = pal_nvm_read((uint32_t)offset, buffer, size);
       val_unlock(&nvm_lock);
@@ -91,6 +109,10 @@ uint32_t val_nvm_write(size_t offset, void *buffer, size_t size)
           return VAL_STATUS_INVALID;
       }
 
+      if (size == 0U) {
+          return VAL_SUCCESS;
+      }
+
       val_lock(&nvm_lock);
       status = pal_nvm_write((uint32_t)offset, buffer, size);
       val_unlock(&nvm_lock);
